# What is a Module? — Summary

A module is a self-contained unit of code with a module-info.java descriptor. Three types exist: Named modules (have module-info.java), Automatic modules (JARs without module-info, name derived from filename), and Unnamed modules (code on classpath, no module name). Named modules are the goal for proper organization.
